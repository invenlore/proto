# Protobuf invenlore.io files

### Generate proto:
```shell
# Generate all protos
make

# or
make generate

# Generate specific proto
make generate/user
```

All files will generated at ./[name]/gen/go/[name]
